#include "pch.h"
#include "Bullet.h"
#include "GameManager.h"

void Bullet::OnCollision(Entity* other)
{
	//if bullet collides with another entity, destroy the bullet
}

void Bullet::OnInitialize(float x, float y, float speed)
{
	
}

void Bullet::position(int x, int y, float speed)
{
	speed = 1.0f;
}

